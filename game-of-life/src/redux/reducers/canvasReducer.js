export const initialCanvasState = {
    canvasWidth: 300,
    canvasHeight: 300,
};

export const canvasReducer = (state = initialCanvasState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};